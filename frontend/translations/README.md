# Translation Support Tools

export to a TSV using the command

`groovy convert export <lang code> > output.tsv`

convert back from a TSV to JSON with the command

`groovy convert import <lang code> <file> > output.json`



